    <h1>Pokémon Checker for X and Y</h1>
    <div class="connection-status">
        <span id="connected" class="label label-success">Connected</span>
	<span id="disconnected" class="label label-warning">Disconnected</span>
    </div>
    <div class="row">
      <div id="left-pokemon" class="col-md-6">
        {{#if left_pokemon }}
        {{>pokemon left_pokemon }}
        {{else}}
        Oops, no pokemon captured yet
        {{/if}}
      </div>
      <div id="right-pokemon" class="col-md-6">
        {{#if right_pokemon }}
        {{>pokemon right_pokemon }}
        {{else}}
        Oops, no pokemon captured yet
        {{/if}}
      </div>
    </div>
    <div class="row">
      <div class="col-md-6">
        <select id="left-list">
        {{#if left_pokemon }}
        <option value="{{json left_pokemon}}">{{>pokemonTitle left_pokemon }}</option>
        {{/if}}
        </select>
      </div>
      <div class="col-md-6">
        <select id="right-list">
        {{#if left_pokemon }}
        <option value="{{json right_pokemon}}">{{>pokemonTitle right_pokemon }}</option>
        {{/if}}
        </select>
      </div>
    </div>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/config.js"></script>
    <script src="/pokemon.js"></script>
